"""
Se quiser criar uma lista de números, você pode converter os resultados de
range() diretamente em uma lista usando a função list(). Quando colocamos
list() em torno de uma chamada à função range(), a saída será uma lista de
números.

Podemos usar list() para converter esse mesmo conjunto de números em uma lista
"""

numeros = list(range(1,6))
